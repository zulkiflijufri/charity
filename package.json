{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": { // perintah yg dapat dijalankan dengan npm run
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "start": "sirv public"
  },
  "devDependencies": { // running in development mode
    "@rollup/plugin-commonjs": "^12.0.0",
    "@rollup/plugin-node-resolve": "^8.0.0",
    "rollup": "^2.3.4",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^5.1.2",
    "svelte": "^3.0.0"
  },
  "dependencies": { // running in production
    "sirv-cli": "^1.0.0"
  }
}
